# ---------------------------------------------------------------------------
# Core pipeline requirements.
#
# These install cleanly on CPU-only machines (including Intel macOS) and are
# enough to run: data curation, benchmark construction, evaluation metrics,
# LLM-as-judge, the A/B inference server and report generation.
#
# For GPU training (QLoRA on a 7B/8B base model) additionally install
# requirements-gpu.txt on a CUDA host.
# ---------------------------------------------------------------------------

# --- config / plumbing ---
pyyaml>=6.0.1
pydantic>=2.7
typer>=0.12
rich>=13.7
python-dotenv>=1.0.1
tqdm>=4.66

# --- data ---
datasets>=2.19
pandas>=2.2
numpy>=1.26

# --- modelling (CPU wheels are fine; CUDA hosts get the cu12 build) ---
torch>=2.4
transformers>=4.44
peft>=0.12
trl>=0.9.6
accelerate>=0.33
sentencepiece>=0.2
safetensors>=0.4.3

# --- evaluation ---
sacrebleu>=2.4
Levenshtein>=0.25
scipy>=1.13

# --- figures ---
matplotlib>=3.8

# --- experiment tracking ---
wandb>=0.17
mlflow>=2.14

# --- serving ---
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27

# --- LLM-as-judge providers (either one is optional at runtime) ---
anthropic>=0.34
openai>=1.40

# --- tests ---
pytest>=8.2
