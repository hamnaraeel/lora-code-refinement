# Sweep arm 2 — the baseline configuration all other arms are read against.
extends: ../qlora_mistral7b.yaml
name: sweep-r16-lr2e4-e3
lora: {r: 16, alpha: 32}
train: {learning_rate: 2.0e-4, num_epochs: 3}
