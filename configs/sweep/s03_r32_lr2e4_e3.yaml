# Sweep arm 3 — high rank. Doubles adapter capacity at the same update scale.
extends: ../qlora_mistral7b.yaml
name: sweep-r32-lr2e4-e3
lora: {r: 32, alpha: 64}
train: {learning_rate: 2.0e-4, num_epochs: 3}
